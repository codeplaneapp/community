Tests run:
1. `bun test e2e/tui/workflow-sse.test.ts --timeout 30000` -> `0 pass, 23 fail, 1 error` (all failures start with TUI exiting: `stdin is not a TTY`).
2. `bun run --cwd apps/tui check` -> fails; includes new hook import errors in TUI wrapper files.
3. `bun run --cwd packages/ui-core check` -> fails; includes new workflow hook type/import/auth errors.

Findings (highest severity first):
1. Cross-package type imports break `ui-core` build and violate architecture layering. `ui-core` now imports from TUI app paths: [useWorkflowLogStream.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowLogStream.ts):13, :14 and [useWorkflowRunSSE.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowRunSSE.ts):11, :12.
2. TUI wrappers import a non-resolvable package subpath: [useWorkflowLogStream.ts](/Users/williamcory/codeplane/specs/tui/apps/tui/src/hooks/useWorkflowLogStream.ts):2 and [useWorkflowRunSSE.ts](/Users/williamcory/codeplane/specs/tui/apps/tui/src/hooks/useWorkflowRunSSE.ts):1 use `@codeplane/ui-core/hooks/workflows`, which is not exported/resolved in this workspace.
3. `APIClient` does not expose `getToken`, but both new hooks call it: [useWorkflowLogStream.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowLogStream.ts):234 and [useWorkflowRunSSE.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowRunSSE.ts):177. This is a compile-time error and breaks bearer fallback logic.
4. Auth fallback header likely mismatches existing client auth scheme (`token ...` in [createAPIClient.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/client/createAPIClient.ts):15). New hooks hardcode `Bearer ...` at [useWorkflowLogStream.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowLogStream.ts):236 and [useWorkflowRunSSE.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowRunSSE.ts):179.
5. `useWorkflowRunSSE` reconnect churn bug: `connectToStream` depends on `runStatuses` ([useWorkflowRunSSE.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowRunSSE.ts):248), and the effect depends on `connectToStream` ([useWorkflowRunSSE.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowRunSSE.ts):280). Every status update can tear down and recreate the stream.
6. Stale closure risk in reconnection callbacks: `initiateReconnection` is memoized with `[]` but calls `connectToStream` in both hooks ([useWorkflowLogStream.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowLogStream.ts):78-105 and [useWorkflowRunSSE.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowRunSSE.ts):107-134). After input changes, reconnect can target stale params.
7. E2E suite is not runnable in current harness: [helpers.ts](/Users/williamcory/codeplane/specs/tui/e2e/tui/helpers.ts):197-203 launches with pipes, while app hard-requires TTY at [terminal.ts](/Users/williamcory/codeplane/specs/tui/apps/tui/src/lib/terminal.ts):9-19. Added workflow SSE tests therefore fail before validating hook behavior.
8. E2E scenario assumptions do not match current app state: workflow screens are placeholders in [registry.ts](/Users/williamcory/codeplane/specs/tui/apps/tui/src/router/registry.ts):122-133, so tests asserting run-detail streaming behavior cannot pass meaningfully.
9. `HOOK-WFSS-031` timeout is mathematically inconsistent with hook config: test waits 30s ([workflow-sse.test.ts](/Users/williamcory/codeplane/specs/tui/e2e/tui/workflow-sse.test.ts):214-225) but hook max reconnect policy (20 attempts, backoff to 30s) needs far longer before reaching `failed`.
10. Test quality gaps: imported helper is unused ([workflow-sse.test.ts](/Users/williamcory/codeplane/specs/tui/e2e/tui/workflow-sse.test.ts):3), many cases are snapshot-only without deterministic SSE stimulus, and no hook-level tests exist for the new `ui-core` hooks.
11. Contract/data nits: `WorkflowStreamConnectionState` includes `"errored"` ([workflow-stream-types.ts](/Users/williamcory/codeplane/specs/tui/apps/tui/src/hooks/workflow-stream-types.ts):57-64) but new hooks never set it; `StepState.log_count` is initialized but never incremented ([useWorkflowLogStream.ts](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowLogStream.ts):176-182).
12. Repository hygiene nit: backup artifact [useWorkflowRunSSE.ts.bak](/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/workflows/useWorkflowRunSSE.ts.bak) should not be committed.

Verification notes:
1. In the modified `apps/tui/src/hooks/*` files, data access is routed through `@codeplane/ui-core` hooks; no direct API/fetch calls were introduced there.
2. The modified files do not directly use OpenTUI components/hooks; no direct OpenTUI misuse was found in these specific changes.
3. Keyboard sequences used in new helpers/tests (`g f`, `j`, `Enter`, `q`, `R`, `G`) are aligned with the design spec, but runtime behavior could not be validated due the TTY/harness blockers above.