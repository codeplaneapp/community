Research lacks critical technical depth required for E2E scaffolding. Missing: (1) Specific @codeplane/ui-core hook signatures (useAgentSessions, useAgentStream, etc.) that tests must mock/validate against, (2) Specific file/line references for OpenTUI APIs (useKeyboard KeyEvent shape, useTerminalDimensions return type), (3) <markdown> component location and streaming API at context/opentui/packages/core/src/renderables/Markdown.ts lines 86-87, (4) Existing AgentMessage types at apps/tui/src/screens/Agents/types.ts, (5) @microsoft/tui-test actual API surface, (6) SSE event format for agent streaming, (7) Braille spinner characters for streaming state detection. The research incorrectly states packages/ui-core is 'empty' when it does not exist. Research must identify concrete hook/component APIs with line numbers, not just generic capabilities. Research must examine OpenTUI source files to extract exact interfaces, not just cite README files.