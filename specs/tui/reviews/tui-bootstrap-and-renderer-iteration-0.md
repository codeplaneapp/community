Tests run:
1. `bun run check` (failed). Notable errors include [GlobalKeybindings.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/components/GlobalKeybindings.tsx:19), [APIClientProvider.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/providers/APIClientProvider.tsx:2), and [ScreenRouter.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/router/ScreenRouter.tsx:15).
2. `bun test ../../e2e/tui/app-shell.test.ts --timeout 30000 --bail 1` (failed immediately: cannot resolve `@codeplane/ui-core`; first test `NAV-SNAP-001` failed waiting for `Dashboard`).

Findings (ordered by severity):
1. Critical: go-to keybindings are broken and currently non-compiling. [GlobalKeybindings.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/components/GlobalKeybindings.tsx:19) indexes `goToBindings` (an array) with a string key, causing TS7015 and runtime lookup failure; navigation logic at [GlobalKeybindings.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/components/GlobalKeybindings.tsx:22) also resets directly to target screen instead of preserving expected stack semantics.
2. Critical: module resolution for `@codeplane/ui-core` is not wired, so app bootstrap is not runnable. Imports exist in [APIClientProvider.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/providers/APIClientProvider.tsx:2), but `@codeplane/ui-core` is missing from [apps/tui/package.json](/Users/williamcory/codeplane/specs/tui/apps/tui/package.json:12).
3. Critical: router does not pass screen params to screen components. [ScreenRouter.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/router/ScreenRouter.tsx:15) renders `<ScreenComponent />` without params, breaking placeholder/title behavior and deep-link context rendering.
4. Critical: bootstrap ordering claim is incorrect. In [index.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/index.tsx:15), `assertTTY()` is called before later static imports, but ESM imports are evaluated before runtime statements; this means TTY guard is not actually guaranteed to run before OpenTUI/import graph side effects.
5. High: provider tree is incomplete for data access. [index.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/index.tsx:50) mounts Theme/Auth/SSE/Navigation, but never mounts API client provider, so hooks expecting API client context cannot function.
6. High: direct API call bypasses shared data/client layer. [AuthProvider.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/providers/AuthProvider.tsx:57) calls `fetch` directly, violating the stated requirement to use `@codeplane/ui-core` data/client abstractions for access.
7. High: global keyboard model does not match spec. [GlobalKeybindings.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/components/GlobalKeybindings.tsx:11) handles `q`, `Esc`, `Ctrl+C`, and partial `g` flow only; no `?` help toggle, no `:` command palette activation, and no modal/overlay-aware Esc handling.
8. High: multiple UI paths hard-exit process instead of graceful teardown. [GlobalKeybindings.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/components/GlobalKeybindings.tsx:13), [TerminalTooSmallScreen.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/components/TerminalTooSmallScreen.tsx:6), and [ErrorBoundary.tsx](/Users/williamcory/codeplane/specs/tui/apps/tui/src/components/ErrorBoundary.tsx:63) call `process.exit()` directly, bypassing centralized cleanup.
9. Medium: signal handler registration leaks listeners across repeated mounts. [signals.ts](/Users/williamcory/codeplane/specs/tui/apps/tui/src/lib/signals.ts:35) uses `process.on(...)` without unregistering; no teardown function is returned.
10. Medium: text truncation is not terminal-cell-width safe. [text.ts](/Users/williamcory/codeplane/specs/tui/apps/tui/src/util/text.ts:12) relies on JS string length, which mis-measures wide/combining Unicode and can cause breadcrumb/status overflow at tight widths.

Given hard compile/runtime failures and multiple spec mismatches, this is not close to LGTM.