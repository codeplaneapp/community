Tests run:
- `bun run check` (in `apps/tui`): pass
- `bun run test:e2e` (in `apps/tui`): fail (277 pass / 536 fail). Primary failure: `Cannot find module '@microsoft/tui-test/lib/terminal/term.js'` from `/Users/williamcory/codeplane/e2e/tui/helpers.ts:289`.
- `bun test ../../e2e/tui/app-shell.test.ts --timeout 30000` (in `apps/tui`): fail (170 pass / 18 fail), same module-resolution error.

Findings (ordered by severity):
1. [Critical] Data-access contract is not implemented. `/Users/williamcory/codeplane/apps/tui/src/providers/APIClientProvider.tsx:3-11` is explicitly a mock client, not `@codeplane/ui-core`, and it is not mounted in the root tree at `/Users/williamcory/codeplane/apps/tui/src/index.tsx:47-60`. This violates the requirement to use shared `@codeplane/ui-core` hooks/client and avoid direct ad-hoc API wiring.
2. [Critical] Auth bootstrap is incorrect and likely misclassifies valid sessions as offline. `/Users/williamcory/codeplane/apps/tui/src/providers/AuthProvider.tsx:48` calls `${apiUrl}/api/v1/user`, but server routes expose `/api/user` (`/Users/williamcory/codeplane/apps/server/src/routes/users.ts:438`). Also token resolution in `AuthProvider.tsx:26-30` only checks env/pre-resolved token and does not implement keyring/config fallback despite declaring `AuthSource` values.
3. [High] Keyboard behavior does not match the design spec. Global handlers in `/Users/williamcory/codeplane/apps/tui/src/components/GlobalKeybindings.tsx:11-49` do not implement `?` (help overlay) or `:` (command palette). Repo-required go-to destinations are not enforced (`GlobalKeybindings.tsx:21-23`), and deep-link resolution also allows repo-scoped screens without repo context (`/Users/williamcory/codeplane/apps/tui/src/navigation/deepLinks.ts:30-34`).
4. [High] Lifecycle teardown is bypassed in multiple UI paths via direct `process.exit`, which skips centralized shutdown semantics in `/Users/williamcory/codeplane/apps/tui/src/lib/signals.ts:25-33`. Direct exits are in `/Users/williamcory/codeplane/apps/tui/src/components/GlobalKeybindings.tsx:13,37,46`, `/Users/williamcory/codeplane/apps/tui/src/components/TerminalTooSmallScreen.tsx:6`, and `/Users/williamcory/codeplane/apps/tui/src/components/ErrorBoundary.tsx:64`.
5. [High] SSE layer is a stub, not a provider implementation. `/Users/williamcory/codeplane/apps/tui/src/providers/SSEProvider.tsx:8-15` stores `null`, `useSSE()` ignores `channel`, and there is no ticket auth, reconnect/backoff, or cursor replay behavior.
6. [Medium] Router/navigation type safety is intentionally disabled. `/Users/williamcory/codeplane/apps/tui/src/router/types.ts:2` uses `screen: string`, registry is cast `as any` in `/Users/williamcory/codeplane/apps/tui/src/navigation/screenRegistry.tsx:35`, and invalid routes silently render `/Users/williamcory/codeplane/apps/tui/src/screens/PlaceholderScreen.tsx:2` via `/Users/williamcory/codeplane/apps/tui/src/router/ScreenRouter.tsx:9-12`.
7. [Medium] Documented stack invariant is not enforced. `MAX_STACK_DEPTH` exists at `/Users/williamcory/codeplane/apps/tui/src/util/constants.ts:24`, but push/reset logic in `/Users/williamcory/codeplane/apps/tui/src/providers/NavigationProvider.tsx:21-35` never applies it.
8. [Medium] OpenTUI hook usage is incomplete against the stated design constraints. Shell uses only `useTerminalDimensions` (`/Users/williamcory/codeplane/apps/tui/src/components/AppShell.tsx:1-10`); there is no explicit `useOnResize` wiring for layout events and no timeline-based hook usage for streaming/animated states.
9. [Low] CLI arg parsing is brittle. `/Users/williamcory/codeplane/apps/tui/src/lib/terminal.ts:28-33` increments argv without validating presence of values for `--repo`/`--screen`, so malformed input silently produces undefined params.
10. [Low] Error boundary implementation has maintainability issues: runtime `require` inside ESM component (`/Users/williamcory/codeplane/apps/tui/src/components/ErrorBoundary.tsx:60`) and hardcoded colors instead of theme tokens (`ErrorBoundary.tsx:70-75`).

Verdict: not LGTM.