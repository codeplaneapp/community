1. Blocker: tests do not pass. Running `bun test e2e/tui/workspaces.test.ts --timeout 30000` produced `11 pass / 9 fail`; all `BADGE-E2E-*` tests timed out waiting for `Workspaces`, and stderr repeatedly shows unresolved `@codeplane/ui-core` during TUI boot.
2. High: E2E tests are not cleanup-safe. `await terminal.terminate()` is placed after assertions instead of in `try/finally`, so failures leak processes/resources (e.g. `/Users/williamcory/codeplane/specs/tui/e2e/tui/workspaces.test.ts:133`, `:143`, `:155`, `:166`, `:175`, `:184`, `:202`, `:217`, `:225`).
3. High: `BADGE-E2E-002` says it verifies label suppression at 80x24, but there is no negative assertion for `Running`; it only checks presence of a dot (`/Users/williamcory/codeplane/specs/tui/e2e/tui/workspaces.test.ts:148`).
4. Medium: The E2E file is coupled to internals (`_STATUS_CONFIG_FOR_TESTING`) and checks implementation details rather than user-facing behavior, conflicting with the PRD testing philosophy (`/Users/williamcory/codeplane/specs/tui/e2e/tui/workspaces.test.ts:8`, `/Users/williamcory/codeplane/specs/tui/apps/tui/src/components/WorkspaceStatusBadge.tsx:71`).
5. Medium: “green/muted/error” claims are not actually validated; assertions only match glyph/label text, not ANSI/color output (`/Users/williamcory/codeplane/specs/tui/e2e/tui/workspaces.test.ts:138`, `:170`, `:179`).
6. Medium: `WorkspaceDisplayStatus` duplicates shared status typing instead of extending the canonical type, creating drift risk (`/Users/williamcory/codeplane/specs/tui/apps/tui/src/components/WorkspaceStatusBadge.tsx:19` vs `/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/types/workspaces.ts:1`).
7. Nit/perf risk: static badges still subscribe through `useSpinner(false)`; when any spinner is active, snapshot frames change and can trigger unnecessary updates (`/Users/williamcory/codeplane/specs/tui/apps/tui/src/components/WorkspaceStatusBadge.tsx:121`, `/Users/williamcory/codeplane/specs/tui/apps/tui/src/hooks/useSpinner.ts:51`, `:137`).

OpenTUI primitives/hooks usage in the badge itself (`<box>`, `<text>`, `fg`, `useTheme`, `useLayout`, `useSpinner`) is syntactically correct, `g w` matches the go-to keybinding spec, and there are no direct API calls in the modified files.