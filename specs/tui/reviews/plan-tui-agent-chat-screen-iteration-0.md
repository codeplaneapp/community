Not LGTM.
1. Critical: Phase 1 targets the wrong registry. The active navigation surface already uses `/Users/williamcory/codeplane/specs/tui/apps/tui/src/navigation/screenRegistry.ts:150` (plus deep links and palette), not `router/screens.ts` alone. See `/Users/williamcory/codeplane/specs/tui/apps/tui/src/navigation/deepLinks.ts:22` and `/Users/williamcory/codeplane/specs/tui/apps/tui/src/commands/agentCommands.ts:1`.
2. Critical: Routing `handleReplay()` to `AgentChat` is a behavioral regression versus the existing replay path. Current wiring/spec surface expects `AgentSessionReplay` (`/Users/williamcory/codeplane/specs/tui/apps/tui/src/navigation/screenRegistry.ts:169`, `/Users/williamcory/codeplane/specs/tui/apps/tui/src/navigation/deepLinks.ts:183`, existing stub intent at `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/AgentSessionListScreen.tsx:114`).
3. Critical: Phase 3 depends on `useSSEChannel(...)`, but no implemented SSE hook/provider exists in this workspace (only a future stub note: `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/hooks/useSessionListSSE.ts:14`).
4. High: ui-core hook contracts are underspecified in the plan. `useSendAgentMessage` requires `CreateAgentMessageRequest` payload (`/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/agents/useSendAgentMessage.ts:23`) and already performs optimistic temp-ID handling (`:88`), but the plan does not define reconciliation to avoid double optimistic entries.
5. High: Message model mismatch is unaddressed. Local agent message shape (`/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/types.ts:16`) differs from ui-core wire shape (`/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/types/agents.ts:47`), yet the plan extends local types without a normalization step.
6. High: 501 polling fallback is not implementable as written with current stream hook behavior. Connection-level SSE HTTP errors are internally retried and not surfaced with status-code detail to consumer callbacks (`/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/hooks/agents/useAgentStream.ts:278`).
7. High: Keyboard plan is incomplete versus spec requirements. It omits explicit handling for global `?`/`:` and chat-required keys/priority chains (`Tab`/`Shift+Tab`, `n/N`, `R`, `Ctrl+D/U`, `Esc` precedence). See baseline design keys at `/Users/williamcory/codeplane/specs/tui/design.md:27` and chat-specific list at `/Users/williamcory/codeplane/specs/tui/TUI_AGENT_CHAT_SCREEN.md:42`.
8. Medium: OpenTUI hook usage is incomplete in the plan. Chat spec requires concrete use of `useOnResize` and `useTimeline` (`/Users/williamcory/codeplane/specs/tui/TUI_AGENT_CHAT_SCREEN.md:252`), but plan only concretely calls out sizing and generic key dispatch.
9. Medium: Verification command is ambiguous/incorrect at repo root. Root package has no `test:e2e` script (`/Users/williamcory/codeplane/specs/tui/package.json:10`); script exists in app package only (`/Users/williamcory/codeplane/specs/tui/apps/tui/package.json:7`).
10. Medium: Test phase lacks execution specificity. It says “append 124 tests” but does not define fixture/server updates for SSE/error states; current `e2e/tui/agents.test.ts` is largely placeholder-comment tests (e.g., `/Users/williamcory/codeplane/specs/tui/e2e/tui/agents.test.ts:6`).
What is correct: scope paths are mostly in `apps/tui/src` and `e2e/tui`, but the above blockers make the plan non-executable as-is.