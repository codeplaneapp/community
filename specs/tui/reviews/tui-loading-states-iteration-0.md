Findings (severity-ordered):
1. Critical — The loading system is not wired into real screen flows. The router still renders static text-only screen components, so new loading hooks/components are effectively unused (/Users/williamcory/codeplane/apps/tui/src/navigation/screenRegistry.tsx:21, /Users/williamcory/codeplane/apps/tui/src/router/ScreenRouter.tsx:15).
2. Critical — `useScreenLoading` can drop errors: when loading ends it calls `completeLoading(id)` first, then error handling runs and often cannot set error because state was removed (/Users/williamcory/codeplane/apps/tui/src/hooks/useScreenLoading.ts:93, /Users/williamcory/codeplane/apps/tui/src/hooks/useScreenLoading.ts:102).
3. Critical — `useScreenLoading` depends on the entire `loading` context object, but provider `value` is not memoized. This causes effect re-runs/re-registration churn during normal state updates (/Users/williamcory/codeplane/apps/tui/src/hooks/useScreenLoading.ts:96, /Users/williamcory/codeplane/apps/tui/src/providers/LoadingProvider.tsx:193).
4. High — Pagination spinner will often be blank: `usePaginationLoading` reads `loading.spinnerFrame`, but spinner frames are only driven by screen/mutation loading in provider; pagination never activates spinner state (/Users/williamcory/codeplane/apps/tui/src/hooks/usePaginationLoading.ts:106, /Users/williamcory/codeplane/apps/tui/src/providers/LoadingProvider.tsx:37).
5. High — `useOptimisticMutation` exposes non-reactive `isLoading` (ref only), and sync throws from `mutate` bypass `.catch`, leaving state inconsistent (/Users/williamcory/codeplane/apps/tui/src/hooks/useOptimisticMutation.ts:52, /Users/williamcory/codeplane/apps/tui/src/hooks/useOptimisticMutation.ts:63).
6. High — Global retry wiring is fragile: `GlobalKeybindings` callback closure omits `retryCallback` dependency (stale callback risk), while `useScreenLoading` registers retry even outside error state (R can trigger unexpectedly) (/Users/williamcory/codeplane/apps/tui/src/components/GlobalKeybindings.tsx:66, /Users/williamcory/codeplane/apps/tui/src/hooks/useScreenLoading.ts:128).
7. High — `currentScreenLoading` is documented as “most recent” but uses first `Map.find`, not most-recent selection (/Users/williamcory/codeplane/apps/tui/src/providers/LoadingProvider.tsx:187).
8. High — Re-registering the same loading id does not clear prior timer before replacing map entry, risking timer/controller leaks and stale timeout side effects (/Users/williamcory/codeplane/apps/tui/src/providers/LoadingProvider.tsx:75).
9. Medium — `ActionButton` exposes `onPress` but never uses it; component is visual-only and not actually actionable (/Users/williamcory/codeplane/apps/tui/src/components/ActionButton.tsx:13, /Users/williamcory/codeplane/apps/tui/src/components/ActionButton.tsx:42).
10. Medium — New E2E loading tests contain weak/incorrect assertions and escaping bugs (e.g., splitting on literal `\\n`, ANSI regex uses escaped literals, many `snapshot.length > 0` checks). This is not a robust test suite (/Users/williamcory/codeplane/e2e/tui/app-shell.test.ts:3510, /Users/williamcory/codeplane/e2e/tui/app-shell.test.ts:4038, /Users/williamcory/codeplane/e2e/tui/app-shell.test.ts:3661).
11. Validation failed: `npx nx run @codeplane/tui:test:e2e` fails (241 pass, 735 fail). Major blockers include unresolved `@microsoft/tui-test/lib/terminal/term.js` from `e2e/tui/helpers.ts` and existing compile failures. `cd apps/tui && bun run check` also fails; touched file path is implicated at /Users/williamcory/codeplane/apps/tui/src/index.tsx:63.
12. Data access requirement check: no direct fetch/axios calls were introduced in the ticket files, but the TUI currently does not use `@codeplane/ui-core` data hooks for these loading flows (and `APIClientProvider` is explicitly a mock: /Users/williamcory/codeplane/apps/tui/src/providers/APIClientProvider.tsx:3).