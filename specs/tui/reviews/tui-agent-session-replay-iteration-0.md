Findings (ordered by severity):
1. [BLOCKER] Keyboard input handling is wired incorrectly in replay. `useKeyboard` receives a `KeyEvent` object, but the code compares the whole object to strings (`key === "j"`, `key === "Escape"`, etc.), so replay keybindings do not execute as intended. See `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/AgentSessionReplayScreen.tsx:83-126` (also corroborated by TS errors at lines 85+).
2. [BLOCKER] Workflow navigation from replay is not implementable in current code. Replay calls `push(ScreenName.WorkflowRunDetail, ...)` but `WorkflowRunDetail` is not defined in `ScreenName`. See `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/AgentSessionReplayScreen.tsx:123` and `/Users/williamcory/codeplane/specs/tui/apps/tui/src/navigation/screenRegistry.ts:10-34`.
3. [BLOCKER] Message shape mismatch between `@codeplane/ui-core` and replay renderer is hidden by unsafe cast. Replay does `rawMessages as AgentMessage[]` without adapting fields; renderer expects `timestamp` and `part.type`, while ui-core hook/types provide `createdAt` and `partType/content`. See `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/AgentSessionReplayScreen.tsx:43`, `/Users/williamcory/codeplane/specs/tui/packages/ui-core/src/types/agents.ts:47-54`, and `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/components/MessageBlock.tsx:67,95-143`.
4. [HIGH] Replay interaction requirements are stubbed/not implemented: search next/prev are TODOs, message jump `[`/`]` are TODOs, tool expand key handling `x/X` is TODO, copy key `y` is TODO. See `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/AgentSessionReplayScreen.tsx:88-92,117-121`. Related state is dead (`scrollOffset`, `currentMessageIndex`, `matchIndex`, `matchCount`, `workflowLinkFocused` not meaningfully updated): lines `51-59` and status display `249` stays incorrect.
5. [HIGH] OpenTUI API usage is inconsistent/invalid in modified files (`color`, `bold`, `borderTop/borderBottom/borderLeft`, `inverse`), causing compile-time failures in these paths and violating the requirement to use OpenTUI primitives correctly. See `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/AgentSessionReplayScreen.tsx:131,139,147,155,167,231,246` and `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/components/SessionSummary.tsx:57,72`.
6. [MEDIUM] Error/retry behavior is incomplete versus design expectations. `R` only refetches messages, not session (`sessionError` path cannot recover), and message-level 401 falls to generic "Failed to load messages" instead of auth-expired guidance. See `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/AgentSessionReplayScreen.tsx:113-114,136-158`.
7. [MEDIUM] Minimum-size handling is incorrect: breakpoint is force-cast to supported values, so `<80x24` does not show unsupported-terminal handling. See `/Users/williamcory/codeplane/specs/tui/apps/tui/src/screens/Agents/AgentSessionReplayScreen.tsx:27` and breakpoint contract at `/Users/williamcory/codeplane/specs/tui/apps/tui/src/types/breakpoint.ts:16-23`.
8. [HIGH] Test implementation quality/regression signal is poor:
   - Replay-targeted run failed immediately: `bun test ../../e2e/tui/agents.test.ts -t "TUI_AGENT_SESSION_REPLAY" --bail --timeout 30000` => `waitForText("REPLAY")` timeout at `/Users/williamcory/codeplane/specs/tui/e2e/tui/agents.test.ts:1711`.
   - Full agents run also failed early (`waitForText("Agent Sessions")`).
   - `e2e/tui/agents.test.ts` still contains 120 empty tests (`async () => {}`), so suite coverage claims are overstated (examples start at `/Users/williamcory/codeplane/specs/tui/e2e/tui/agents.test.ts:451`).
   - Some replay assertions do not validate claimed behavior (e.g., KEY-REPLAY-029 expects `/j/` after typing `j` in search rather than asserting no scroll change) at `/Users/williamcory/codeplane/specs/tui/e2e/tui/agents.test.ts:2329-2333`.

Verification summary:
- Ran tests: `bun test ../../e2e/tui/agents.test.ts --timeout 30000` (failing), `bun test ../../e2e/tui/agents.test.ts -t "TUI_AGENT_SESSION_REPLAY" --bail --timeout 30000` (fails on first replay snapshot), and `bun run check` (typecheck fails, including replay/session-summary specific errors).
- Data-access requirement check: replay does use `@codeplane/ui-core` hooks (no direct API calls), but current adapter/casting is unsafe and functionally incorrect.