Not LGTM. Findings: 1) Keyboard model mismatch: Step 3 omits required global bindings (`Esc` to close/back and `Ctrl+C` immediate quit) and does not define precedence with local keys (`s`, `j/k`, `?`). 2) OpenTUI hook usage is underspecified: responsive behavior is described, but the plan does not explicitly require `useTerminalDimensions` + `useOnResize` wiring for live resize correctness. 3) Provider architecture is internally inconsistent: placing `HeaderBar`/`StatusBar` outside the boundary while boundary sits above `NavigationProvider`/`SSEProvider` likely removes needed context (breadcrumb, sync, notifications) or yields stale state; no concrete data-flow design is provided. 4) `@codeplane/ui-core` continuity is not explicit: restart/reset should define how existing ui-core hooks/query state are remounted/refreshed and how in-flight requests are handled; current wording is too broad. 5) Crash loop behavior is incomplete: constants and ring buffer are listed, but behavior at limit reached, exact 5s boundary semantics, and reset semantics after stable runtime are not specified. 6) Test-crash hook safety gap: importing/mounting `TestCrashHook` in `PlaceholderScreen` needs explicit non-test build gating; env-var checks alone are insufficiently strict for production safety. 7) Test plan gaps: no explicit assertions for `Esc`, help overlay dismissal/focus trapping, crash-loop lockout messaging, stderr telemetry/log JSON shape, and hard-fallback/double-fault behavior contract. 8) Specificity issue: Step 1 contains ambiguous language (`if needed`, `if already exists`) rather than deterministic implementation instructions and acceptance criteria. Path targeting is mostly correct (`apps/tui/src/*`, `e2e/tui/*`), but these gaps block approval.